<% unless @question.body.empty? &&  @question.title.empty? %>
  $('.question-<%= @question.id %>').html('<%= j render @question %>')
<% end %>

$('.flash').html('<%= j render 'shared/notice' %>')
$('#edit-question').prepend('<%= j render 'shared/errors', object: @question %>')
