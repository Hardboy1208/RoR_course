= form_tag search_path, method: :get do
  = search_field_tag :q, params[:q]
  = select_tag "search_class", options_for_select([ "all", "question", "answer", "comment" ], "all")
  = submit_tag "Search"
